import React from 'react';
import ListaReportes from '../components/ListaReportes';

const VerReportes = () => (
  <div className="row">
    <ListaReportes />
  </div>
);

export default VerReportes;
